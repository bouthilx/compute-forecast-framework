[pytest]
pythonpath = .
testpaths = tests
addopts = -v
asyncio_mode = auto
markers =
    asyncio: mark test as an async test.

# Test performance options
# Global timeout of 5 minutes per test
timeout = 300
timeout_method = thread
